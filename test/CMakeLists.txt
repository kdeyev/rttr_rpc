# for compiling all examples with only one target
#add_custom_target(examples ALL COMMENT "compile all examples")
                                    
#set_target_properties(examples PROPERTIES FOLDER "Examples")
                  
add_subdirectory (rttRpc)